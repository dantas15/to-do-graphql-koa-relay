import { TodoAddMutation } from './todo-add-mutation';
import { TodoUpdateMutation } from './todo-update-mutation';

export const todoMutations = {
  TodoAdd: TodoAddMutation,
  TodoUpdate: TodoUpdateMutation,
};
