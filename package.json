{
  "private": true,
  "name": "todo-graphql-koa-relay",
  "scripts": {
    "config:local": "turbo run config:local",
    "dev": "turbo run dev",
    "compose:up": "docker-compose -f docker-compose.yaml up -d",
    "compose:down": "docker-compose -f docker-compose.yaml down",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.4"
  },
  "packageManager": "pnpm@9.4.0",
  "pnpm": {
    "patchedDependencies": {
      "graphql-relay@0.10.1": "patches/graphql-relay@0.10.1.patch"
    }
  }
}
